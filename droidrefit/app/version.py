# Firmware version — the single source of truth. Bump this, commit, push to
# `main`, and every droid's OTA check() will offer the update.
# Format: YYYY.MM.DD or YYYY.MM.DD.N for same-day revisions.

VERSION = "2026.08.31"
